#ifndef REN_GLSL_KEYWORDS_GLSL
#define REN_GLSL_KEYWORDS_GLSL

#define static_assert(expr)

#define inline

#define alignas(alignment)

#define glsl_restrict restrict

#endif // REN_GLSL_KEYWORDS_GLSL
